# ローカルでは Vite のプロキシを経由（/api → backend:8000）
# Vite 側で x-api-key をヘッダー注入できるため、401 を回避
VITE_API_BASE_URL=""
VITE_API_BASE_PREFIX=""
VITE_API_PATH_PREFIX="/api"

# プロキシが使うバックエンドURLとAPIキー
VITE_BACKEND_URL="http://localhost:8000"
# 既存の MY_API_KEY を流用（vite.config.js が読み取ります）
